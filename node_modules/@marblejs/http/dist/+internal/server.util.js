"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getServerAddress = exports.closeServer = void 0;
const closeServer = (server) => () => new Promise((res, rej) => server.close(err => err ? rej(err) : res(undefined)));
exports.closeServer = closeServer;
const getServerAddress = (server) => {
    const serverAddressInfo = server.address();
    const host = serverAddressInfo.address === '::' ? '127.0.0.1' : serverAddressInfo.address;
    const port = serverAddressInfo.port;
    return { host, port };
};
exports.getServerAddress = getServerAddress;
