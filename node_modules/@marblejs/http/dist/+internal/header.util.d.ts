import * as O from 'fp-ts/lib/Option';
import { HttpHeaders } from '../http.interface';
/**
 * Get header value for given key from provided headers object
 *
 * @param key header key
 * @since 4.0.0
 */
export declare const getHeaderValue: <T extends string = string>(key: string) => (headers: HttpHeaders) => O.Option<T>;
/**
 * Normalize HTTP headers (transform keys to lowercase)
 *
 * @param headers
 * @since 4.0.0
 */
export declare const normalizeHeaders: (headers: HttpHeaders) => HttpHeaders;
//# sourceMappingURL=header.util.d.ts.map